'use strict';

module.exports.getReplies = async function (event, context) {
  // this function should receive an identifier

  // should query dynamodb for the message, iterate over replies list and get those messages to format them
};
